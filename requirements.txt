google-genai>=1.65.0,<2
customtkinter>=5.2.0
sounddevice>=0.4.6
numpy>=1.24.0
pillow>=10.0.0
opencv-python>=4.8.0
insightface>=0.7.3
python-dotenv>=1.0.0
sqlalchemy>=2.0
mako>=1.3.0
alembic>=1.13
fastembed>=0.3

# Local LLM (llama.cpp) chat console
llama-cpp-python>=0.3.0
huggingface-hub>=0.28

# Task scheduling (cron expressions)
croniter>=2.0

# Hardware UART / Serial communication with ESP32
pyserial>=3.5

# Neural Noise Suppression (RNNoise)
pyrnnoise>=0.1.0

# Custom offline wake-word inference
onnxruntime>=1.10.0,<2
scipy>=1.3,<2
scikit-learn>=1,<2
tqdm>=4,<5
requests>=2,<3
# Linux installation is handled with --no-deps by setup_pi.sh because
# openWakeWord 0.6.0 requires an unused tflite-runtime wheel unavailable on
# Python 3.13/aarch64. Cubey uses the explicitly selected ONNX backend.
openwakeword==0.6.0; platform_system != "Linux"

# Web Server & Real-Time Map Remote Control
fastapi>=0.110.0
uvicorn>=0.28.0
python-multipart>=0.0.9
websockets>=12.0
